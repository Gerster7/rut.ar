$kind: http-request
name: 'Buscar Negocios de Retorno'
method: GET
url: '{{baseUrl}}/api/viajes/{{viajeId}}/negocios-retorno'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
